import Foundation
import Network

public enum PairingControlActions {
    public static let sessionOpen = "session.open"
    public static let sessionProperties = "session.properties"
    public static let sessionComplete = "session.complete"
    public static let clientLog = "client.log"
    public static let deviceProfile = "device.profile"
    public static let appState = "app.state"
}

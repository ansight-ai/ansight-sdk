# AnsightLocation

Optional explicit recording of app-observed coordinates over an existing Ansight session.
The module never requests location permission, creates a location manager, or installs a delegate.

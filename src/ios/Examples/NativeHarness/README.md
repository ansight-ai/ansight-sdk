# Ansight iOS Native Harness

This harness app is defined with `project.yml` so the project file does not need to be hand-maintained.

To generate the Xcode project, install `xcodegen` and run:

```sh
xcodegen generate
```

Then open the generated `AnsightNativeHarness.xcodeproj` and run the app.

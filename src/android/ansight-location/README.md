# Ansight Location for Android

This optional AAR records app-owned `android.location.Location` values over the existing Ansight
session. It declares no permissions and does not create a location manager or install listeners.

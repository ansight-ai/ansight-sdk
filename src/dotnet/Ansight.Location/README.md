# Ansight.Location

Optional, explicit app-observed location recording over the existing Ansight session. Capture is off by default and includes precision, interval, and distance controls. See the [location guide](../../../docs/location.md).

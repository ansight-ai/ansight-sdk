# Ansight.Native.Android.Binding

Internal slim binding package for the Ansight Android runtime.

The managed surface is generated from `ansight-dotnet-bridge`. The native
`ansight-core` AAR is packaged as an implementation dependency without exposing
its Kotlin object model as public C# API.
